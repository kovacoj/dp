#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
latexmk=(docker compose -f "$repo_root/containers/compose.yaml" run --rm latex latexmk)

if ! "${latexmk[@]}" thesis "$@"; then
  "${latexmk[@]}" -g thesis "$@"
fi
