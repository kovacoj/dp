% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sketch_lstsq_multi.R
\name{sketch_lstsq_multi}
\alias{sketch_lstsq_multi}
\title{Solve a multivariate least-squares problem with a randomized sketch}
\usage{
sketch_lstsq_multi(
  A,
  Y,
  s,
  method = c("gaussian", "rademacher", "srht"),
  seed = NULL,
  return_sketch = FALSE
)
}
\arguments{
\item{A}{Numeric design matrix with more rows than columns.}

\item{Y}{Numeric right-hand side matrix with `nrow(Y) == nrow(A)`.}

\item{s}{Integer sketch size.}

\item{method}{Sketch family. One of `"gaussian"`, `"rademacher"`, or
`"srht"`.}

\item{seed}{Optional integer seed for reproducibility.}

\item{return_sketch}{Logical; if `TRUE`, include the sketch matrix `S`
in the output.}
}
\value{
A list with the solution matrix `X`, residual matrix, Frobenius
  residual norm, sketched-system condition number, sketch size, method,
  and optionally the sketch matrix.
}
\description{
Solves \eqn{\min_X \|AX - Y\|_F} by forming the sketched problem
\eqn{\min_X \|SAX - SY\|_F} and recovering \eqn{X} via QR on \eqn{SA}.
}
\examples{
set.seed(1)
A <- matrix(rnorm(80), nrow = 20)
Y <- matrix(rnorm(60), nrow = 20)
fit <- sketch_lstsq_multi(A, Y, s = 10, method = "gaussian")
fit$residual_norm

}
