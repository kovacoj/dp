% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterative_refine.R
\name{sketch_lstsq_mixed}
\alias{sketch_lstsq_mixed}
\title{Solve a sketched least-squares problem with simulated mixed precision}
\usage{
sketch_lstsq_mixed(
  A,
  b,
  s = NULL,
  method = c("gaussian", "rademacher", "srht"),
  sketch_prec = "double",
  solve_prec = "double"
)
}
\arguments{
\item{A}{Numeric design matrix.}

\item{b}{Numeric response vector with length equal to `nrow(A)`.}

\item{s}{Integer sketch size. If `NULL`, defaults to `min(nrow(A), 2 *
(ncol(A) + 1))`.}

\item{method}{Sketch family. One of `"gaussian"`, `"rademacher"`, or
`"srht"`.}

\item{sketch_prec}{Precision used for the sketch and sketched products.
Passed to [fl_round()].}

\item{solve_prec}{Precision used before solving the sketched system. Passed
to [fl_round()].}
}
\value{
A list with the coefficient vector `x`, residual diagnostics,
  sketched-system condition number, sketch settings, and precision settings.
}
\description{
Builds a randomized sketch, rounds selected intermediate quantities to
simulated lower precision, and solves the resulting sketched least-squares
problem.
}
\examples{
set.seed(1)
A <- matrix(rnorm(80), nrow = 20)
b <- rnorm(20)
fit <- sketch_lstsq_mixed(A, b, s = 10, sketch_prec = "single")
fit$residual_norm

}
