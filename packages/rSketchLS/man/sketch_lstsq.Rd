% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sketch_ls.R
\name{sketch_lstsq}
\alias{sketch_lstsq}
\title{Solve a least-squares problem with a randomized sketch}
\usage{
sketch_lstsq(A, b, s = NULL, method = c("gaussian", "rademacher", "srht"))
}
\arguments{
\item{A}{Numeric design matrix with more rows than columns.}

\item{b}{Numeric response vector with length equal to `nrow(A)`.}

\item{s}{Integer sketch size. If `NULL`, defaults to `min(nrow(A), 2 *
(ncol(A) + 1))`.}

\item{method}{Sketch family. One of `"gaussian"`, `"rademacher"`, or
`"srht"`.}
}
\value{
A list with the coefficient vector `x`, residual vector,
  residual norm, sketched-system condition number, sketch size, and method.
}
\description{
Forms a sketching matrix, solves the compressed least-squares problem
\eqn{\min_x \|SAx - Sb\|_2}, and reports diagnostics on the residual in
the original problem.
}
\examples{
set.seed(1)
A <- matrix(rnorm(80), nrow = 20)
b <- rnorm(20)
fit <- sketch_lstsq(A, b, s = 10, method = "gaussian")
fit$residual_norm

}
