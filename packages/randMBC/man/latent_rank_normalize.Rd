% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_rank_normalize.R
\name{latent_rank_normalize}
\alias{latent_rank_normalize}
\title{Rank-normalize and inverse-normal transform a data matrix}
\usage{
latent_rank_normalize(values, reference = NULL, ties_method = "average")
}
\arguments{
\item{values}{Numeric matrix to transform.}

\item{reference}{Numeric matrix used to define the empirical CDF for each
column. If `NULL`, `values` is used as its own reference.}

\item{ties_method}{Character. `"average"` or `"first"`, controlling how
ties are broken during ranking. Default `"average"`.}
}
\value{
A numeric matrix of the same dimensions as `values`, on the
  latent Gaussian scale.
}
\description{
Applies plotting-position ranking, then transforms to a standard-normal
scale via the inverse-normal CDF. Columns are centered after the transform.
}
\examples{
set.seed(1)
x <- matrix(rnorm(60), nrow = 20, ncol = 3)
z <- latent_rank_normalize(x)
all(abs(colMeans(z)) < 0.3)
}
