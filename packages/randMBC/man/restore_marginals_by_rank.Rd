% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latent_rank_normalize.R
\name{restore_marginals_by_rank}
\alias{restore_marginals_by_rank}
\title{Restore marginal distributions from a latent-score matrix}
\usage{
restore_marginals_by_rank(marginals, scores, ties_method = "average")
}
\arguments{
\item{marginals}{Numeric matrix whose sorted columns supply the target
marginal values.}

\item{scores}{Numeric matrix whose columnwise ranks determine the
rearrangement.}

\item{ties_method}{Character. `"average"` or `"first"`. Default `"average"`.}
}
\value{
A numeric matrix with the marginals of `marginals` and the
  dependence structure induced by `scores`.
}
\description{
Replaces the marginal distributions of `target_values` with those from
`marginals`, rearranged according to the columnwise ranks of `scores`.
This is the inverse step of [latent_rank_normalize()].
}
\examples{
set.seed(1)
x <- matrix(rnorm(60), nrow = 20, ncol = 3)
z <- latent_rank_normalize(x)
y <- restore_marginals_by_rank(x, z)
max(abs(sort(x[, 1]) - sort(y[, 1])))
}
